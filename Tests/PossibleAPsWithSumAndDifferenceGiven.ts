import NumberyStuff from "../Source/Index";

const Sum = 12;
const Difference = 1;

console.log(`The number of possible APs with the Sum ${Sum} and Difference ${Difference} is ${(NumberyStuff.PossibleAPsWithSumAndDifferenceGiven(Sum, Difference))}`);